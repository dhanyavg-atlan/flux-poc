apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base/podinfo
  - ../base/poc-app
patches:
  - path: podinfo-values.yaml
    target:
      kind: HelmRelease
      name: podinfo          # scope to podinfo only — otherwise this patch also hits poc-app
